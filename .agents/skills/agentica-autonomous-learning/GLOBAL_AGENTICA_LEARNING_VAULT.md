# GLOBAL AGENTICA LEARNING VAULT

This file serves as the permanent, autonomous learning brain for Agentica.
It contains strict rules regarding OS safety, file system management, and operational workflows.

## Reinforcement & Safety Directives
1. **Always read this file before executing complex workflows or destructive terminal commands.**
2. **Never violate the rules and operational guidelines appended below.**

## Extracted Learnings
- [2026-06-27] System Initialized. Awaiting first operational feedback loop.
- **Strict Security Rule Adherence:** NEVER attempt to break, bypass, or rewrite hardcoded security sandboxes or guards (like modifying `prompts.ts` destructively or running restricted terminal commands). Always respect the system's security architecture.
- **Semantic Prompt Chunking:** When building or manipulating large prompts (or fixing prompt scripts), ALWAYS use proper semantic boundaries (like `\n\n`) to ensure that chunking logic does not break XML tags, instructions, or sentences. This is critical for preventing prompt corruption.
